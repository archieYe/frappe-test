[project]
name = "user-growth"
version = "0.0.1"
description = "Frappe app for user growth and churn analytics"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"